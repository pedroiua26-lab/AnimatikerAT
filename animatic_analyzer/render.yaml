services:
  - type: web
    name: animatic-analyzer-api
    env: python
    plan: free
    rootDir: animatic_analyzer
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
